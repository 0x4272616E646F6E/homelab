apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: janitorr-https-route
  namespace: media
spec:
  parentRefs:
    - name: envoy-gateway
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - "janitorr.oglesby.io"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: janitorr
          port: 8978
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: janitorr-http-route
  namespace: media
spec:
  parentRefs:
    - name: envoy-gateway
      namespace: envoy-gateway-system
      sectionName: http
  hostnames:
    - "janitorr.oglesby.io"
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301