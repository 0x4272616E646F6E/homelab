apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: subgen-models
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 16Gi