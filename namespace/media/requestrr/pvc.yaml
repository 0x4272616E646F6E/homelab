apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: requestrr-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 8Gi