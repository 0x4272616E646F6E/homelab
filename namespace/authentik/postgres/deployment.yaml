apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentik-postgres-15
  namespace: authentik
  labels:
    app: authentik-postgres-15
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authentik-postgres-15
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: authentik-postgres-15
    spec:
      securityContext:
        fsGroup: 999
      containers:
      - name: postgres
        image: postgres:17.6-bookworm@sha256:acd32fd91bbdda025a0cc6d9de5666282dfea5d2f30ca7899c1c787f78062121
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_DB
          value: "authentik"
        - name: POSTGRES_USER
          value: "authentik"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: authentik-postgres-secrets
              key: POSTGRES_PASSWORD
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
          subPath: postgres
        resources:
          limits:
            memory: 1Gi
            cpu: "1"
          requests:
            memory: 256Mi
            cpu: 200m
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: data-authentik-postgresql-0