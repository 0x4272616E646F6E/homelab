apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wikipedia-https
  namespace: mcp
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`mcp.oglesby.io`) && PathPrefixStrip(`/wikipedia`)
      kind: Rule
      middlewares:
        - name: internal-whitelist
          namespace: traefik
      services:
        - name: wikipedia-mcp
          port: 80
  tls:
    secretName: oglesby-io-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wikipedia-http
  namespace: mcp
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`mcp.oglesby.io`) && PathPrefixStrip(`/wikipedia`)
      kind: Rule
      middlewares:
        - name: internal-whitelist
          namespace: traefik
        - name: redirect-https
          namespace: traefik
      services:
        - name: wikipedia-mcp
          port: 80