apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: playwright-https
  namespace: mcp
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`mcp.oglesby.io`) && PathPrefixStrip(`/playwright`)
      kind: Rule
      middlewares:
        - name: internal-whitelist
          namespace: traefik
      services:
        - name: playwright
          port: 80
  tls:
    secretName: oglesby-io-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: playwright-http
  namespace: mcp
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`mcp.oglesby.io`) && PathPrefixStrip(`/playwright`)
      kind: Rule
      middlewares:
        - name: internal-whitelist
          namespace: traefik
        - name: redirect-https
          namespace: traefik
      services:
        - name: playwright
          port: 80