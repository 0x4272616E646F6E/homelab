apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comfyui-config
  namespace: llm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 8Gi