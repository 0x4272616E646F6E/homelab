apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lucy-128k-pvc
  namespace: llm
  labels:
    app.kubernetes.io/name: lucy-128k
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 8Gi