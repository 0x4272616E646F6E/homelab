apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lucy-128k-pvc
  namespace: llm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 8Gi