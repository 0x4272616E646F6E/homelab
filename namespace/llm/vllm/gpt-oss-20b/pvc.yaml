apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gpt-oss-20b-pvc
  namespace: llm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 40Gi