apiVersion: apps/v1
kind: Deployment
metadata:
  name: llama-cpp
  namespace: llm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llama-cpp
  template:
    metadata:
      labels:
        app: llama-cpp
    spec:
      initContainers:
        - name: model-downloader
          image: alpine:latest
          command:
          - sh
          - -c
          - |
            apk add --no-cache curl && \
            echo "Checking if model exists..." && \
            if [ ! -f /models/qwen3.gguf ]; then
              echo "Downloading model..."
              curl -L -o /models/qwen3.gguf "https://huggingface.co/unsloth/Qwen3-1.7B-GGUF/resolve/main/Qwen3-1.7B-Q4_1.gguf?download=true"
            else
              echo "Model already exists, skipping download."
            fi
          volumeMounts:
            - name: models
              mountPath: /models
      containers:
        - name: llama-cpp
          image: ghcr.io/ggml-org/llama.cpp:server-intel
          args:
          - "-m"
          - "/models/qwen3.gguf"
          - "--port"
          - "3000"
          - "--host"
          - "0.0.0.0"
          - "--ctx-size"
          - "1024"
          - "--n-gpu-layers"
          - "20"
          ports:
            - containerPort: 3000
              protocol: TCP
          env:
          - name: GGML_SYCL_DISABLE_GRAPH
            value: "0"
          - name: GGML_SYCL_DISABLE_OPT
            value: "0"
          - name: GGML_SYCL_DEBUG
            value: "0"
          volumeMounts:
            - name: models
              mountPath: /models
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "2"
              memory: "4Gi"
              gpu.intel.com/i915: "1"
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: llama-cpp-models