apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: glance-config
  namespace: glance
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 8Gi