apiVersion: apps/v1
kind: Deployment
metadata:
  name: filebrowser
  namespace: media
spec:
  replicas: 1
  selector:
    matchLabels:
      app: filebrowser
  template:
    metadata:
      labels:
        app: filebrowser
    spec:
      runtimeClassName: youki
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        initContainers:
        - name: init-config
          image: busybox:1.36@sha256:303337d3d52898b33018796d760d3a01c2dcc87973dd66ec22fe78ae1e8429c9
          imagePullPolicy: Always
          command:
            - sh
            - -c
            - |
              if [ ! -f /config/config.json ]; then
                echo "Creating default config.json"
                cat <<EOF > /config/config.json
              {
                "port": 80,
                "baseURL": "",
                "address": "",
                "log": "stdout",
                "database": "/database/filebrowser.db",
                "root": "/srv"
              }
              EOF
              fi
          volumeMounts:
            - name: config
              mountPath: /config
      containers:
        - name: filebrowser
          image: docker.io/filebrowser/filebrowser:s6@sha256:d3a20347e4d0cc567089d0734dee8c3dd47f540c4e2f4a9364e20a4f33ee2b09
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          volumeMounts:
            - name: config
              mountPath: /config
            - name: db
              mountPath: /database
            - name: fileshare
              mountPath: /srv
              subPath: fileshare
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: filebrowser-config
        - name: db
          persistentVolumeClaim:
            claimName: filebrowser-db
        - name: fileshare
          persistentVolumeClaim:
            claimName: fileshare