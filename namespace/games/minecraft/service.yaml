apiVersion: v1
kind: Service
metadata:
  name: minecraft
  namespace: games
  labels:
    app.kubernetes.io/name: minecraft
spec:
  selector:
    app.kubernetes.io/name: minecraft
  ports:
    - port: 25565
      targetPort: 25565
      protocol: TCP
      name: minecraft
  type: ClusterIP