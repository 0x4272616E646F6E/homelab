apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-config
  namespace: games
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-rwo
  resources:
    requests:
      storage: 8Gi