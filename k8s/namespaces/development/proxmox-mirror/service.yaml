apiVersion: v1
kind: Service
metadata:
  name: proxmox-mirror
  namespace: development
  labels:
    app.kubernetes.io/name: proxmox-mirror
    app.kubernetes.io/managed-by: flux
spec:
  selector:
    app.kubernetes.io/name: proxmox-mirror
    app.kubernetes.io/managed-by: flux
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  type: ClusterIP
