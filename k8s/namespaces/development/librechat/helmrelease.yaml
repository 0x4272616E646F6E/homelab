# renovate: docker=ghcr.io/danny-avila/librechat-chart/
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: librechat
  namespace: development
spec:
  chart:
    spec:
      chart: librechat
      version: 1.9.0
      sourceRef:
        kind: HelmRepository
        name: librechat
        namespace: flux-system
  interval: 15m
  timeout: 5m
  releaseName: librechat
  valuesFrom:
  - kind: ConfigMap
    name: librechat-config-map
    valuesKey: values.yaml
