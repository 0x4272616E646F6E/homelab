apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarqube-postgres-pvc
  namespace: development
  labels:
    app.kubernetes.io/name: sonarqube-postgres-17
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: local-path-rwo
  volumeMode: Filesystem
