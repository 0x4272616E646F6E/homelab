apiVersion: v1
kind: Service
metadata:
  name: talos-mirror
  namespace: development
  labels:
    app.kubernetes.io/name: talos-mirror
    app.kubernetes.io/managed-by: flux
spec:
  selector:
    app.kubernetes.io/name: talos-mirror
    app.kubernetes.io/managed-by: flux
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  type: ClusterIP
