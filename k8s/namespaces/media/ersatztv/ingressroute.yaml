apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-ersatztv-https
  namespace: media
  labels:
    app.kubernetes.io/name: ersatztv
    app.kubernetes.io/managed-by: flux
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`ersatztv.oglesby.io`)
      kind: Rule
      middlewares:
        - name: internal-whitelist
          namespace: ingress
        - name: authentik
          namespace: ingress
      services:
        - name: ersatztv
          port: 8409
  tls: 
    secretName: oglesby-io-tls
