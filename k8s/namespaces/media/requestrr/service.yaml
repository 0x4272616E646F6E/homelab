apiVersion: v1
kind: Service
metadata:
  name: requestrr
  namespace: media
  labels:
    app.kubernetes.io/name: requestrr
    app.kubernetes.io/managed-by: flux
spec:
  selector:
    app.kubernetes.io/name: requestrr
    app.kubernetes.io/managed-by: flux
  ports:
    - port: 4545
      targetPort: 4545
      protocol: TCP
      name: requestrr-ui
  type: ClusterIP
