apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: subgen-models
  namespace: media
  labels:
    app.kubernetes.io/name: subgen
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 16Gi