apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wizarr-config
  namespace: media
  labels:
    app.kubernetes.io/name: wizarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 8Gi