apiVersion: v1
kind: Service
metadata:
  name: clamav
  namespace: security
  labels:
    app.kubernetes.io/name: clamav
    app.kubernetes.io/component: daemon
    app.kubernetes.io/managed-by: flux
spec:
  type: ClusterIP
  ports:
    - name: clamd
      port: 3310
      targetPort: 3310
      protocol: TCP
  selector:
    app.kubernetes.io/name: clamav
    app.kubernetes.io/component: daemon
