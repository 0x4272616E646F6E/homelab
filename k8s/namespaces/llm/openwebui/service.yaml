apiVersion: v1
kind: Service
metadata:
  name: openwebui
  namespace: llm
  labels:
    app.kubernetes.io/name: openwebui
spec:
  selector:
    app.kubernetes.io/name: openwebui
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP