apiVersion: v1
kind: ConfigMap
metadata:
  name: metrics-server-config-map
  namespace: kube-system
data:
  values.yaml: |-
    # Metrics Server Configuration - Only Custom Values
    # Chart defaults are used for all settings
    # All settings match chart defaults - no customization needed
    {}
