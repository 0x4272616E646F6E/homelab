apiVersion: v1
kind: ConfigMap
metadata:
  name: metrics-server-config-map
  namespace: kube-system
data:
  values.yaml: |-
    # Metrics Server Configuration
    args:
      - --kubelet-insecure-tls
