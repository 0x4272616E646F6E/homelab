apiVersion: apps/v1
kind: Deployment
metadata:
  name: talos-debug
  namespace: kube-system
  labels:
    app.kubernetes.io/name: talos-debug
    app.kubernetes.io/managed-by: flux
spec:
  replicas: 1
  revisionHistoryLimit: 3
  progressDeadlineSeconds: 600
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: talos-debug
  template:
    metadata:
      labels:
        app.kubernetes.io/name: talos-debug
    spec:
      nodeName: talos-4m3-8nj
      securityContext:
        runAsUser: 0
      containers:
      - name: talos-debug
      # renovate: datasource=docker depName=docker.io/alpine
        image: docker.io/alpine:3.22.1@sha256:eafc1edb577d2e9b458664a15f23ea1c370214193226069eb22921169fc7e43f
        command: ["sh", "-c", "echo 'host debug pod ready'; sleep 36000"]
        securityContext:
          privileged: true
        volumeMounts:
        - name: hostroot
          mountPath: /host
        resources:
          requests:
            cpu: "1"
            memory: "1Gi"
          limits:
            cpu: "2"
            memory: "2Gi"
      volumes:
      - name: hostroot
        hostPath:
          path: /
          type: Directory