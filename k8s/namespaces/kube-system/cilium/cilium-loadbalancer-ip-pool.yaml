apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: cilium-lb-pool
  namespace: kube-system
spec:
  blocks:
    - cidr: 10.0.0.249/32
    - cidr: 10.0.0.248/32
    - cidr: 10.0.0.247/32
