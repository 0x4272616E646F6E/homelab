# renovate: registryUrl=https://stakater.github.io/stakater-charts
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: reloader
  namespace: reloader
spec:
  chart:
    spec:
      chart: reloader
      version: 2.2.3
      sourceRef:
        kind: HelmRepository
        name: reloader
        namespace: flux-system
  interval: 15m
  timeout: 5m
  releaseName: reloader
  valuesFrom:
  - kind: ConfigMap
    name: reloader-config-map
    valuesKey: values.yaml
