apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-glance-https
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`glance.oglesby.io`)
      kind: Rule
      middlewares:
        - name: internal-whitelist
          namespace: ingress
      services:
        - name: glance
          port: 80
  tls: 
    secretName: oglesby-io-tls
