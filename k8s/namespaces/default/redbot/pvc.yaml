apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redbot-config
  namespace: default
  labels:
    app.kubernetes.io/name: redbot
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-rwo
  resources:
    requests:
      storage: 8Gi
