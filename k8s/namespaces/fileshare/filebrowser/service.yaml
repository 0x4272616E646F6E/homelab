apiVersion: v1
kind: Service
metadata:
  name: filebrowser
  namespace: media
  labels:
    app.kubernetes.io/name: filebrowser
spec:
  selector:
    app.kubernetes.io/name: filebrowser
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: filebrowser-ui
  type: ClusterIP