apiVersion: v1
kind: Service
metadata:
  name: sabnzbd
  namespace: download
spec:
  selector:
    app: sabnzbd
  ports:
    - port: 8080
      targetPort: 8080
      protocol: UDP
      name: sabnzbd-ui
  type: ClusterIP