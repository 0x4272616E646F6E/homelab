apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gluetun-config
  namespace: download
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-static
  resources:
    requests:
      storage: 5Gi