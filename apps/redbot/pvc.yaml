apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redbot-config
  namespace: automation
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-static
  resources:
    requests:
      storage: 5Gi